# Core dependencies
torch>=2.0
numpy>=1.23
matplotlib>=3.5

# Benchmark / baseline dependencies (synthetic tasks + baselines)
scikit-learn>=1.2
scipy>=1.9

# Real-EEG benchmark (PhysioNet motor imagery). Optional but required to
# reproduce the VPC manuscript's real-data results.
mne>=1.5

# Optional extras (install only if you run the corresponding material):
#   pip install .[bci]        # brainflow  — live-BCI notebooks 3.5-3.7
#   pip install .[qiskit]     # qiskit     — Shor comparison in ex_03
#   pip install .[notebooks]  # jupyter/nbclient/ipykernel — run the notebooks
#   pip install .[all]        # everything above

# Testing
pytest>=7.0
